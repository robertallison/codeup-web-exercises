'use strict'
/* var i = 0;
while (i < 10) {
    console.log(i);
    i++;
}
This is just a practice on while loops, actual problems are below
 */

 /* function skipNumber() {
    var stopNum = prompt("Please input an odd number from 1 to 50 to omit" );
    var skipNum = parseInt(stopNum)
      if(skipNum % 2 !== 0) {
          for (var i = 1; i <= 50; i += 2) {
              if (i === skipNum) {
                  console.log("Yikes skipping number: " + i);
                  continue;
              }
              console.log(i)
          }
      } else {
          alert("Sorry that is not in the bounds")
      }
}
skipNumber();
*/